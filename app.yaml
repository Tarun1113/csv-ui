command: ["/bin/bash", "-c", "streamlit run app.py --server.port 
$DATABRICKS_APP_PORT --server.headless true --server.address 0.0.0.0 --
server.enableCORS false --server.enableXsrfProtection false"]
 
